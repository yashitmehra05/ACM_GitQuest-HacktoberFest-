#include <stdio.h>

int main() {
    char str1[100];
    int i, f= 0;

    printf("Enter the first string: ");
    gets(str1); 
    for (i = 0; str1[i] != '\0'; i++) {
        str[i]=str[] 
        }
    
    return 0;
}
